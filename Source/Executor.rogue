module Boss

class Executor : Visitor
  PROPERTIES

  METHODS
    method on_visit( cmd:Print )
      print ((forEach in cmd.args)->String)

    method on_visit( cmd:Println )
      print ((forEach in cmd.args)->String)
      println
endClass

