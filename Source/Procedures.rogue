module Boss

class Procedures
  PROPERTIES
    by_name = StringTable<<Procedure[]>>()
    by_signature = StringTable<<Procedure>>()

  METHODS
    method add( def:Procedure )
      if (not by_name.contains(def.name)) by_name[def.name] = Procedure[]
      by_name[ def.name ].add( def )
endClass
